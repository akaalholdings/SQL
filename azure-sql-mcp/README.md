# Azure SQL MCP moved

The Azure SQL MCP server is maintained in the public
[`akaalholdings/azure-sql-mcp`](https://github.com/akaalholdings/azure-sql-mcp)
repository.

This directory is retained only as a migration pointer. It contains no server
implementation, package metadata, or MCP CI workflow.
